Greetings!

We need you to verify the project <%= @project.name %>.  The projects details are as follows:

Organization: <%= @project.name %>
Type(s): 
Goals: <%= @project.goals %>


If this project is valid, you can verify it by clicking the link below:

<%= project_verify_url(@project.verification_token) %>

Once verified, <%= @project.name %> will appear on the public-facing AustinProBono.org website at <%= organization_project_url(@project.organization, @project) %>


Sincerely,
The AustinProBono.org Website
